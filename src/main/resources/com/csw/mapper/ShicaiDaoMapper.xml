<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.csw.dao.ShicaiDao">
    <select id="queryAll" resultType="Shicai">
    select * from shicai
</select>
    <insert id="add">
        insert into shicai values(#{c.id},#{c.name})
    </insert>
</mapper>
